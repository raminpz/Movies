services:
  mysql:
    image: mysql:8
    container_name: movies-mysql
    environment:
      MYSQL_DATABASE: movieDb
      MYSQL_ROOT_PASSWORD: rami123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    restart: always

volumes:
  db_data: